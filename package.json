{
	"name": "runner-api",
	"version": "1.0.0",
	"description": "",
	"license": "ISC",
	"author": "",
	"type": "module",
	"main": "index.js",
	"scripts": {
		"build": "node build-tasks.js build-remote",
		"start": "node build-tasks.js build-local && npm run pb:build-image && npm run pb:start-container",
		"pb:build-image": "docker build --quiet -t runner-api-pocketbase ./pocketbase",
		"pb:start-container": "docker run --rm -p 8090:8090 --env-file pocketbase/.env --name runner-api-pocketbase runner-api-pocketbase"
	},
	"dependencies": {
		"pocketbase": "^0.27.0"
	},
	"devDependencies": {
		"esbuild": "^0.28.1"
	}
}
